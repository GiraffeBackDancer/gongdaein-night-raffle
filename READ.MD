# 공대인의 밤 몬스터볼 추첨 프로그램

> 공대인의 밤(12월 3일) 포켓몬 컨셉 행사에서 사용한 오프라인 경품 추첨 프로그램

## 배경
- 참가자에게 **실물 포켓몬 카드**를 응모권으로 사전 배부 (1인 1장, 모두 다른 포켓몬)
- 무대 스크린에서 볼을 흔들어 포켓몬을 뽑으면, 그 카드를 가진 사람이 당첨
- 대강당은 네트워크가 불안정할 수 있어 **인터넷 없이 동작**해야 했음

## 주요 기능
- 로컬 이미지 폴더를 선택하면 파일명을 포켓몬 이름으로 사용해 후보 등록
- 클릭 또는 키 입력 → 약 2초 흔들림 → 흰 섬광 → 포켓몬 이미지와 이름 등장
- 한 번 뽑힌 포켓몬은 후보에서 자동 제외
- 프레젠터 리모컨 하나로 추첨 → 결과 → 다음 추첨까지 전체 진행

## 사용 방법
1. `assets/ball.png`에 볼 이미지(투명 배경 PNG)를 넣는다
2. 배부한 카드와 **같은 포켓몬 이미지**만 한 폴더에 모은다
   - 파일명이 화면에 그대로 표시됨 (예: `0025-피카츄.png` → `0025-피카츄`)
3. `index.html`을 크롬으로 열고 `F11`로 전체 화면
4. 이미지 폴더 선택 → 추첨 화면으로 전환
5. `스페이스바` / `Enter` / `→` / `PageDown` / 클릭으로 진행

## 설계 근거
**결과를 먼저 정하고, 연출은 결과를 보여주기만 한다**
클릭 순간 당첨 포켓몬이 확정되고 후보에서 제외된다. 흔들림·섬광은 이미 정해진 결과를 향해 진행되므로 연출 도중 결과가 바뀔 수 없다.

**중복 실행 방지: 검사 → 즉시 잠금 → 작업**
`isDrawing` 플래그로 연출 중 클릭·키 입력을 무시한다. "후보가 없음" 같은 실행 불가 조건은 잠그기 **전에** 검사한다. 잠근 뒤 빠져나가면 잠금이 풀리지 않아 이후 입력이 영원히 막히기 때문이다.

**이미지 한 장의 한계를 섬광으로 가림**
볼이 열리는 연출은 평면 이미지로는 안쪽 면을 표현할 수 없어 제외했다. 대신 화면이 완전히 흰 구간(섬광 시작 후 약 0.23~0.53초)에 볼을 포켓몬으로 교체해, 교체 순간이 보이지 않게 했다.

**연출 순서를 이벤트로 연결**
흔들림 종료(`animationend`) → 섬광 시작 → 섬광 종료 → 입력 잠금 해제. 앞 연출이 끝나야 다음이 시작되므로 타이밍이 어긋나지 않는다.

**이미지 미리 불러오기**
포켓몬 이미지 주소를 섬광 때가 아니라 **클릭 순간** 지정해, 2초 흔들림 동안 브라우저가 이미지를 준비하게 했다. 공개 순간 이미지가 늦게 뜨는 문제를 막는다.

**어떤 스크린에서도 같은 비율 (`vmin`)**
행사장 프로젝터의 해상도·비율(16:9, 4:3)을 미리 알 수 없어, 화면의 짧은 변 기준 단위인 `vmin`으로 크기를 지정했다. 사회자만 보는 준비 화면은 `px` 고정.

**리모컨 대응**
프레젠터 리모컨은 제품마다 보내는 키가 달라 `→`, `PageDown`, `스페이스바`, `Enter`를 모두 받는다. 추첨 시작 로직은 `startDraw()` 함수 하나로 묶어 클릭과 키 입력이 같은 코드를 사용한다.

**사용자 실수 대비**
이미지가 없는 폴더를 선택하면 경고 후 준비 화면을 유지한다. 폴더 안의 윈도우 숨김 파일(`Thumbs.db` 등)은 파일 형식으로 걸러낸다.

**외부 의존성 없음**
단일 HTML 파일, 프레임워크·CDN 없음. 폰트도 프로젝트 안에 포함해 오프라인에서 동일하게 보인다.

**저작권**
포켓몬 관련 이미지는 저장소에 포함하지 않는다(`.gitignore`). 볼 이미지와 포켓몬 이미지는 실행 시 로컬에서만 불러온다.

**의도적으로 뺀 것**
컨페티, 당첨 목록 표시, 대기 중 흔들림은 구현을 검토했으나 포켓몬 공개에 집중하기 위해 제외했다.

## 트러블슈팅
| 증상 | 원인 | 해결 |
|---|---|---|
| 다시 뽑기 후 볼과 이전 포켓몬이 함께 보임 | CSS 교체 중 `.reveal { display: none; }` 규칙이 삭제됨 | 숨김 규칙 복구, 짝 없는 중괄호 제거 |
| 스페이스바로 추첨이 시작되지 않음 | 키 입력 코드가 아직 정의되지 않은 `startDraw()`를 호출 (`ReferenceError`) | 클릭 로직을 `startDraw()` 함수로 분리 |
| 연출 중 연타 시 여러 번 추첨됨 | 진행 상태를 확인하지 않음 | `isDrawing` 잠금 추가 |

## 한계와 개선 아이디어
- 볼→포켓몬 교체 시점(`300ms`)이 섬광 애니메이션 길이에 의존한다. 섬광 타이밍을 바꾸면 이 값도 함께 조정해야 한다.
- 새로고침하면 추첨 상태가 초기화된다. 행사 중 실수로 새로고침하면 이미 뽑힌 포켓몬이 후보로 돌아온다.
- 폴더를 다시 고르려면 새로고침이 필요하다.

## 기술 스택
HTML / CSS / JavaScript (프레임워크 없음)

## 폴더 구조
```
gongdaein-night-raffle/
├── index.html
├── fonts/          갈무리 폰트 + 라이선스
├── assets/         볼 이미지 (저장소 미포함)
└── docs/           README용 데모 GIF
```

## 크레딧
- 폰트: [갈무리(Galmuri)](https://github.com/quiple/galmuri) by 이민서, SIL Open Font License 1.1
- 포켓몬은 Nintendo·Creatures·GAME FREAK의 상표이며, 이 저장소에는 관련 이미지가 포함되어 있지 않습니다.